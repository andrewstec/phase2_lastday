@model IEnumerable<WebApplication1.Models.ProductVM>

@{
    ViewBag.Title = @User.Identity.Name + " Farm Inventory";
}

<h2>Farm Inventory</h2>

<table class="table">
    <tr>
        <th>
            Product Name
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            Product Category
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QtyInKG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FarmName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductCategory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QtyInKG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FarmName)
        </td>
    </tr>
}

    <tr>
        <td>@Html.ActionLink("Add a Product", "Add", "Product", null, null)</td>
    </tr>
</table>
